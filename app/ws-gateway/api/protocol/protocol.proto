syntax = "proto3";

package gateway.protocol;

option go_package = "github.com/lyouthzzz/ws-gateway/app/ws-gateway/api/protocol";

enum Type {
  CONNECT = 0;
  DISCONNECT = 1;
  HEARTBEAT = 2;
}

message Protocol {
  string type = 1;
  bytes payload = 2;
}